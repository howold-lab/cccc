# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "cccc-pair-sherpa-onnx-sys"
version = "1.13.4-cccc.1"
build = "build.rs"
links = "sherpa-onnx"
include = [
    "src/**",
    "build.rs",
    "Cargo.toml",
    "README.md",
    "LICENSE*",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "CCCC-maintained sherpa-onnx FFI bindings with verified native archives"
readme = "README.md"
keywords = [
    "ffi",
    "speech",
    "sherpa-onnx",
    "bindings",
]
categories = ["external-ffi-bindings"]
license = "Apache-2.0"
repository = "https://github.com/ChesterRa/cccc"
publish = ["crates-io"]

[package.metadata.docs.rs]
default-features = false
features = []

[features]
default = ["static"]
shared = []
static = []

[lib]
name = "sherpa_onnx_sys"
path = "src/lib.rs"

[build-dependencies.bzip2]
version = "0.4"

[build-dependencies.tar]
version = "0.4"

[build-dependencies.sha2]
version = "0.10"

[build-dependencies.ureq]
version = "2.12"
features = ["proxy-from-env"]
