# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "cccc-pair-sherpa-onnx"
version = "1.13.4-cccc.1"
build = false
include = [
    "src/**",
    "Cargo.toml",
    "README.md",
    "LICENSE*",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "CCCC-maintained safe wrapper for verified sherpa-onnx native archives"
readme = "README.md"
keywords = [
    "speech",
    "speech-to-text",
    "stt",
    "onnx",
    "asr",
]
categories = [
    "api-bindings",
    "multimedia::audio",
]
license = "Apache-2.0"
repository = "https://github.com/ChesterRa/cccc"
publish = ["crates-io"]

[package.metadata.docs.rs]
default-features = false
features = []

[features]
default = ["static"]
shared = ["sherpa-onnx-sys/shared"]
static = ["sherpa-onnx-sys/static"]

[lib]
name = "sherpa_onnx"
path = "src/lib.rs"

[dependencies.serde]
version = "1.0"
features = ["derive"]

[dependencies.serde_json]
version = "1.0"

[dependencies.sherpa-onnx-sys]
package = "cccc-pair-sherpa-onnx-sys"
version = "=1.13.4-cccc.1"
path = "../sherpa-onnx-sys"
default-features = false
